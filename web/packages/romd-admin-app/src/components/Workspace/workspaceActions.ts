import type { ButtonProps } from '@mantine/core';

/** Primary workspace actions; choose filled or light at the call site. */
export const workspaceActionProps = {
  color: 'teal',
  radius: 'sm',
} satisfies ButtonProps;
